import { PurePipeCodePipe } from './pure-pipe-code.pipe';

describe('PurePipeCodePipe', () => {
  it('create an instance', () => {
    const pipe = new PurePipeCodePipe();
    expect(pipe).toBeTruthy();
  });
});
